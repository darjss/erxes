import { Car } from '@/car/graphql/resolvers/customResolvers/car';
import { CarCategory } from '@/car/graphql/resolvers/customResolvers/carCategory';

export const customResolvers = {
  Car,
  CarCategory,
};

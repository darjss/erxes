import { IRelationWidgetProps } from 'ui-modules';

/** Placeholder until customer relation widgets are implemented for Mto. */
export const MtoCustomerWidgets = (_props: IRelationWidgetProps) => {
  return null;
};
